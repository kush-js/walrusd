{
  "name": "@walrus/db",
  "version": "0.1.0",
  "description": "WALrus embedded SQLite runtime binding for Node.js and Bun",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "prebuilds",
    "native",
    "lib"
  ],
  "scripts": {
    "build": "node-gyp rebuild -C native && tsc -p .",
    "test": "bun test ../bun/*.test.ts && node --test smoke.test.mjs e2e.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "node-gyp": "^11.5.0",
    "typescript": "^7.0.2"
  },
  "gypfile": true,
  "license": "MIT"
}
